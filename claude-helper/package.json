{
  "name": "murgatchat-claude-helper",
  "private": true,
  "version": "1.0.0",
  "description": "Pont MurgaChat → Claude expert « supervision » (Agent SDK). Tourne sur la VM claude-help, pas dans docker.",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/index.ts",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.0",
    "express": "^5.1.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
