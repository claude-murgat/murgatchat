node_modules
.expo
dist
web-build
.env.local

# Native projects are generated by `expo prebuild` (CI + local), never committed.
android
ios
# Local keystore materialized from the GitHub secret during CI signing.
upload.keystore
# FCM client config — materialized from a GitHub secret in CI, or dropped in
# locally for a dev build. Carries project identifiers, so kept out of git.
google-services.json
